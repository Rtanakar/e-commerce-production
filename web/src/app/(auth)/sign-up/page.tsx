import { SignUpForm } from "@/features/auth/components/signup-form";

export const metadata = {
  title: "Sign up | Eshop",
  description: "Create your Eshop account",
};

export default function SignUpPage() {
  return <SignUpForm />;
}
